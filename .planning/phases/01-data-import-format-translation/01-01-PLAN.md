---
phase: 01-data-import-format-translation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/config.R
  - R/run_all.R
  - .gitignore
  - .Rprofile
autonomous: true
requirements:
  - INF-01
  - INF-02
  - INF-03
  - INF-04
  - INF-06
  - IMP-04

must_haves:
  truths:
    - "All file paths in the pipeline are parameterized via config.R using here::here() — no hardcoded absolute paths"
    - "run_all.R can execute the full pipeline or resume from any step via start_step parameter"
    - "Project directory structure exists with data/raw, data/processed, output/tables, output/figures, R/ directories"
    - "renv lockfile setup is documented and ready for initialization when R is available"
    - ".gitignore excludes data files, renv library, and output but includes renv.lock and R scripts"
  artifacts:
    - path: "R/config.R"
      provides: "Path parameterization for all pipeline scripts"
      contains: "here::here"
    - path: "R/run_all.R"
      provides: "Master pipeline runner with start_step support"
      contains: "start_step"
    - path: ".gitignore"
      provides: "Git exclusion rules for data, output, renv library"
      contains: "data/"
  key_links:
    - from: "R/run_all.R"
      to: "R/config.R"
      via: "source(here::here('R', 'config.R'))"
      pattern: "source.*config\\.R"
    - from: "R/run_all.R"
      to: "R/01_formats.R"
      via: "scripts vector references all pipeline scripts"
      pattern: "01_formats\\.R"
---

<objective>
Establish project infrastructure for the SAS-to-R conversion pipeline: config.R for path parameterization, run_all.R master runner, .gitignore, and directory structure.

Purpose: Every subsequent script in the pipeline depends on config.R for paths and the established directory structure. This is the foundation that all Phase 1 (and later phases) build upon.

Output: R/config.R, R/run_all.R, .gitignore, directory scaffolding ready for format translation and data import scripts.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-import-format-translation/01-CONTEXT.md
@.planning/phases/01-data-import-format-translation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.R, .gitignore, and directory structure</name>
  <files>R/config.R, .gitignore</files>
  <read_first>
    .planning/phases/01-data-import-format-translation/01-CONTEXT.md
    .planning/phases/01-data-import-format-translation/01-RESEARCH.md
  </read_first>
  <action>
Create the R/ directory, then create R/config.R with the following exact content:

**R/config.R must contain:**
1. `library(here)` at the top
2. A header comment block:
   ```
   # R/config.R
   # Path configuration for Precision Cancer Survivorship pipeline
   # Per D-04: sourced config.R for all path parameterization (no YAML, no .Renviron)
   # Per D-05: paths only — study parameters stay in analysis scripts for auditing
   ```
3. Data directory paths using here::here():
   - `data_dir_raw <- here("data", "raw")` — where SAS7BDAT files live
   - `data_dir_processed <- here("data", "processed")` — where .rds checkpoints go
4. Output directory paths:
   - `output_dir <- here("output")`
   - `output_dir_tables <- here("output", "tables")`
   - `output_dir_figures <- here("output", "figures")`
5. SAS encoding parameter (per Research pitfall 6):
   - `sas_encoding <- "latin1"` with comment: `# Default for clinical data; change to "UTF-8" if text corruption detected`
6. Directory creation block: Use `dir.create(..., recursive = TRUE, showWarnings = FALSE)` for each of: data_dir_raw, data_dir_processed, output_dir_tables, output_dir_figures
7. A comment block documenting SAS library alias confusion per D-10 and Research pitfall 4:
   ```
   # NOTE: SAS library alias confusion (per D-10, Research pitfall 4):
   #   SAS "libname v3" alias actually points to Data_v5 directory
   #   SAS "libname v4" alias points to Data_v4 directory
   #   SAS "libname dx" alias points to Dx directory
   #   R config uses clear names: data_dir_raw (= V5 data), no ambiguous aliases
   ```
8. A final message: `message("Config loaded. Project root: ", here())`

**Do NOT include:**
- Any study-specific parameters (date ranges, ICD codes, age thresholds) per D-05
- Any hardcoded absolute paths — everything via here::here()
- Any YAML or .Renviron loading

**.gitignore must contain these exact entries:**
```
# Data files (HIPAA protected, not in version control)
data/
*.sas7bdat
*.sas7bcat

# R environment
.Rhistory
.RData
.Rproj.user/

# renv library (large, reproducible from lockfile)
renv/library/
renv/staging/
renv/sandbox/
renv/cellar/

# Output files (regenerated by pipeline)
output/

# OS files
.DS_Store
Thumbs.db

# Keep these IN version control:
# renv.lock (package versions)
# renv/activate.R (renv bootstrap)
# renv/settings.json (renv config)
# R/*.R (all pipeline scripts)
# .Rprofile (renv activation)
```

Also create a minimal `.Rprofile` with a comment explaining it will be populated by renv::init():
```
# .Rprofile
# This file will be populated by renv::init() to activate project-local library
# Run renv::init() after installing R to set up reproducible environment
# See INF-06 requirement
```

Per INF-04 (inline documentation), include a comment at top of config.R referencing:
`# SAS BUG FIX: SAS code uses confusing library aliases (v3 for Data_v5). See note below.`
  </action>
  <verify>
    <automated>grep -c "here" R/config.R && grep -c "data_dir_raw" R/config.R && grep -c "data/" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - R/config.R contains `library(here)` on its own line
    - R/config.R contains `data_dir_raw <- here("data", "raw")`
    - R/config.R contains `data_dir_processed <- here("data", "processed")`
    - R/config.R contains `output_dir_tables <- here("output", "tables")`
    - R/config.R contains `output_dir_figures <- here("output", "figures")`
    - R/config.R contains `sas_encoding <- "latin1"`
    - R/config.R contains `dir.create` calls for all directory paths
    - R/config.R contains `# NOTE: SAS library alias confusion`
    - R/config.R does NOT contain any absolute paths (no "C:", no "/blue/", no "/home/")
    - R/config.R does NOT contain study-specific parameters (no ICD codes, no date ranges, no age thresholds)
    - .gitignore contains `data/` line
    - .gitignore contains `*.sas7bdat` line
    - .gitignore contains `renv/library/` line
    - .gitignore contains `output/` line
    - .Rprofile exists and contains `renv::init()` reference
  </acceptance_criteria>
  <done>config.R provides all path definitions using here::here(), .gitignore excludes data and renv library, directory structure scaffolded, no hardcoded paths anywhere</done>
</task>

<task type="auto">
  <name>Task 2: Create run_all.R master pipeline runner</name>
  <files>R/run_all.R</files>
  <read_first>
    R/config.R
    .planning/phases/01-data-import-format-translation/01-CONTEXT.md
  </read_first>
  <action>
Create R/run_all.R with the following exact structure per D-09:

**R/run_all.R must contain:**

1. Header comment block:
   ```
   # R/run_all.R
   # Master runner for Precision Cancer Survivorship pipeline
   # Per D-09: Supports start_step parameter for partial reruns from .rds checkpoints
   # Per INF-02: Executes full pipeline from data import to final outputs
   # Usage: source("R/run_all.R") or Rscript R/run_all.R
   ```

2. `library(here)` — only dependency needed at runner level

3. A `start_step` parameter defaulting to 1:
   ```r
   # Change start_step to resume from a later step (leveraging .rds checkpoints per D-08)
   # Example: start_step <- 2 skips format translation, loads from checkpoint
   start_step <- 1
   ```

4. A `scripts` character vector listing pipeline scripts in execution order:
   ```r
   scripts <- c(
     "01_formats.R",     # Translate Formats.sas to R factor definitions
     "01_import.R"       # Import all V5 SAS7BDAT files
     # Phase 2 scripts will be added here:
     # "02_clean.R",     # Data cleaning and standardization
     # "03_merge.R",     # Dataset merging and joining
     # Phase 3 scripts:
     # "04_cohort.R",    # Cohort construction and exclusion criteria
     # "05_exposure.R",  # Exposure variable derivation
     # "06_outcomes.R",  # Outcome variable calculation
     # "07_covariates.R",# Covariate processing
     # Phase 4 scripts:
     # "08_table1.R",    # Descriptive statistics and Table 1
     # "09_models.R",    # Regression models
     # "10_output.R"     # Final tables and figures
   )
   ```

5. Pipeline banner with timestamp:
   ```r
   message("========================================")
   message("Precision Cancer Survivorship Pipeline")
   message("Started: ", Sys.time())
   message("Starting from step: ", start_step)
   message("Total scripts: ", length(scripts))
   message("========================================")
   ```

6. Execution loop that sources scripts conditionally:
   ```r
   for (i in seq_along(scripts)) {
     if (i >= start_step) {
       script_path <- here("R", scripts[i])
       if (!file.exists(script_path)) {
         message("WARNING: Script not found, skipping: ", scripts[i])
         next
       }
       message("\n>>> Step ", i, "/", length(scripts), ": Running ", scripts[i], " <<<")
       tryCatch({
         source(script_path, local = new.env(parent = globalenv()))
         message("Completed: ", scripts[i], " at ", Sys.time())
       }, error = function(e) {
         message("ERROR in ", scripts[i], ": ", conditionMessage(e))
         message("Pipeline halted. Fix error and rerun with start_step <- ", i)
         stop(e)
       })
     } else {
       message("Skipped (start_step=", start_step, "): ", scripts[i])
     }
   }
   ```
   Note: `local = new.env(parent = globalenv())` prevents scripts from polluting each other's environments while still allowing access to packages.

7. Completion banner:
   ```r
   message("\n========================================")
   message("Pipeline complete: ", Sys.time())
   message("========================================")
   ```

Per INF-04, include inline comments explaining:
- Why start_step exists (D-09: partial reruns from checkpoints)
- Why tryCatch wraps source() (graceful error handling with resume guidance)
- Why script_path uses here() not relative paths (D-04: no hardcoded paths)
- Why Phase 2-4 scripts are commented out (added incrementally per INF-01 modular architecture)
  </action>
  <verify>
    <automated>grep -c "start_step" R/run_all.R && grep -c "01_formats.R" R/run_all.R && grep -c "01_import.R" R/run_all.R && grep -c "here" R/run_all.R</automated>
  </verify>
  <acceptance_criteria>
    - R/run_all.R contains `start_step <- 1` assignment
    - R/run_all.R contains `library(here)` at the top
    - R/run_all.R contains `"01_formats.R"` in the scripts vector
    - R/run_all.R contains `"01_import.R"` in the scripts vector
    - R/run_all.R contains `source(script_path` call inside the loop
    - R/run_all.R contains `tryCatch` error handling
    - R/run_all.R contains `here("R", scripts[i])` for path construction
    - R/run_all.R contains comment referencing D-09
    - R/run_all.R contains commented-out placeholders for Phase 2-4 scripts
    - R/run_all.R does NOT contain any hardcoded absolute paths
    - R/run_all.R does NOT contain setwd() calls
  </acceptance_criteria>
  <done>run_all.R executes all pipeline scripts in order with start_step support for partial reruns, tryCatch error handling with resume guidance, and here-based path construction</done>
</task>

</tasks>

<verification>
1. R/config.R exists and contains only path definitions via here::here(), no hardcoded paths
2. R/run_all.R exists with start_step parameter, scripts vector listing 01_formats.R and 01_import.R
3. .gitignore excludes data/, output/, renv/library/ but would keep renv.lock
4. .Rprofile exists with renv documentation
5. No file in R/ directory contains hardcoded absolute paths (grep for "C:", "/blue/", "/home/" should return nothing)
</verification>

<success_criteria>
- config.R provides complete path parameterization for all pipeline scripts using here::here()
- run_all.R can execute pipeline scripts in sequence with optional resume from checkpoint
- .gitignore properly configured for clinical research R project
- All infrastructure follows locked decisions D-04, D-05, D-08, D-09
- No hardcoded paths exist in any created file
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-import-format-translation/01-01-SUMMARY.md`
</output>
