---
phase: 02-data-cleaning-merging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/helper-fixtures.R
  - tests/testthat/test_02_cleaning.R
  - tests/testthat/test_02_merging.R
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03
  - CLN-04
  - CLN-05
  - CLN-06
  - MRG-01
  - MRG-03
  - MRG-04

must_haves:
  truths:
    - "Test fixtures provide realistic mock PCORnet encounter data for all Phase 2 tests"
    - "Cleaning tests fail correctly (RED) before implementation exists"
    - "Merging tests fail correctly (RED) before implementation exists"
  artifacts:
    - path: "tests/testthat/helper-fixtures.R"
      provides: "Mock encounter, diagnosis, procedure, provider, format fixtures"
      contains: "mock_encounter1"
    - path: "tests/testthat/test_02_cleaning.R"
      provides: "Test stubs for CLN-01 through CLN-06"
      contains: "test_that"
    - path: "tests/testthat/test_02_merging.R"
      provides: "Test stubs for MRG-01, MRG-03, MRG-04"
      contains: "test_that"
  key_links:
    - from: "tests/testthat/helper-fixtures.R"
      to: "tests/testthat/test_02_cleaning.R"
      via: "source() at top of test file"
      pattern: "source.*helper-fixtures"
    - from: "tests/testthat/helper-fixtures.R"
      to: "tests/testthat/test_02_merging.R"
      via: "source() at top of test file"
      pattern: "source.*helper-fixtures"
---

<objective>
Create Wave 0 test infrastructure: mock data fixtures and test scaffolds for all Phase 2 cleaning and merging requirements.

Purpose: Per VALIDATION.md Nyquist compliance, every Phase 2 task needs automated verification. These test files must exist BEFORE implementation so executors can run RED-GREEN-REFACTOR cycles.

Output: Three test files providing mock data and test stubs that will initially FAIL (RED state), ready for Phase 2 implementation to make them pass (GREEN).
</objective>

<execution_context>
@.planning/phases/02-data-cleaning-merging/02-VALIDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@R/01_formats.R
@R/01_import.R

<interfaces>
<!-- Key types and contracts from Phase 1 that test fixtures must match -->

From R/01_formats.R (format definitions used in tests):
```r
sas_formats$p_payer <- list(
  levels = c("5", "1", "2", "3", "4", "8", "9", "U"),
  labels = c("Private", "Medicare", "Medicaid", "Private", "Med_Medicaid",
             "Uninsured", "Other", "Unknown")
)

sas_formats$enc_type <- list(
  levels = c("AV", "ED", "EI", "IP", "IS", "OS", "IC", "TH", "OA", "NI", "UN", "OT"),
  labels = c("Ambulatory Visit", "Emergency Department", ...)
)

sas_formats$discharge_status <- list(
  levels = c("AF", "AL", "AM", "AW", "EX", "HH", "HO", "HS", "IP", "NH",
             "RH", "RS", "SH", "SN", "NI", "UN", "OT"),
  labels = c(...)
)

sas_formats$discharge_disposition <- list(
  levels = c("A", "E", "NI", "UN", "OT"),
  labels = c("Discharged alive", "Expired", "No information", "Unknown", "Other")
)

sas_formats$payerr <- list(
  levels = c("5", "1", "2", "3", "4", "8", "9", "U"),
  labels = c("Private", "Medicare", "Medicaid", "Goverment", "Med_Medicaid",
             "None", "Other", "Unknown")
)
```

From R/01_import.R (checkpoint naming pattern):
```r
# Checkpoints saved as:
#   data/processed/01_imported_demo.rds
#   data/processed/01_imported_enroll.rds
#   data/processed/01_imported_dx_parts.rds (list of 7 tibbles)
#   data/processed/01_imported_proc_parts.rds (list of 4 tibbles)
#   data/processed/01_imported_provider.rds
#   data/processed/01_imported_prov_spec.rds
#   data/processed/01_formats.rds

# import_sas() applies janitor::clean_names() at import time
# Column names are already lowercase snake_case
```

PCORnet CDM column names (post clean_names):
- Encounters: patid, encounterid, enc_type, admit_date, discharge_date, discharge_status, discharge_disposition, payer_type_primary, payer_type_secondary, providerid, facility_type
- Diagnosis: patid, encounterid, diagnosisid, dx, dx_type, dx_source, dx_origin, dx_date, dx_poa, pdx, admit_date, enc_type, providerid
- Procedures: patid, encounterid, proceduresid, px, px_type, px_source, px_date, enc_type, providerid
- Provider: providerid, provider_specialty_primary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixture file with mock PCORnet CDM data</name>
  <files>tests/testthat/helper-fixtures.R</files>
  <read_first>
    - R/01_formats.R (format definitions that fixtures must match)
    - R/01_import.R (column naming conventions from clean_names)
  </read_first>
  <action>
Create directories `tests/testthat/` (recursive). Create `tests/testthat/helper-fixtures.R` with the following mock data fixtures:

1. **mock_sas_formats** — Minimal replica of `sas_formats` object containing only the formats Phase 2 needs:
   - `$p_payer`: levels = c("5", "1", "2", "3", "4", "8", "9", "U"), labels = c("Private", "Medicare", "Medicaid", "Private", "Med_Medicaid", "Uninsured", "Other", "Unknown")
   - `$payerr`: levels = c("5", "1", "2", "3", "4", "8", "9", "U"), labels = c("Private", "Medicare", "Medicaid", "Goverment", "Med_Medicaid", "None", "Other", "Unknown")
   - `$enc_type`: levels = c("AV", "ED", "EI", "IP", "IS", "OS", "IC", "TH", "OA", "NI", "UN", "OT"), labels matching 01_formats.R exactly
   - `$discharge_status`: levels = c("AF", "AL", "AM", "AW", "EX", "HH", "HO", "HS", "IP", "NH", "RH", "RS", "SH", "SN", "NI", "UN", "OT"), labels matching exactly
   - `$discharge_disposition`: levels = c("A", "E", "NI", "UN", "OT"), labels = c("Discharged alive", "Expired", "No information", "Unknown", "Other")

2. **mock_encounter1** — tibble with 5 rows, columns: `patid` (character), `encounterid` (character), `enc_type` (character), `admit_date` (Date), `discharge_date` (Date), `discharge_status` (character), `discharge_disposition` (character), `payer_type_primary` (character), `payer_type_secondary` (character), `providerid` (character), `facility_type` (character). Mix of enc_types: "AV", "IP", "ED". Mix of payer codes: "1" (Medicare), "2" (Medicaid), "5" (Private). Include one row with `payer_type_primary = NA` for missing value testing. Use patid values "P001" through "P005", encounterid values "E001" through "E005".

3. **mock_encounter2** — tibble with 5 rows, same columns as mock_encounter1. Use patid values "P006" through "P010", encounterid values "E006" through "E010". Include one row with `payer_type_primary = "4"` (Dual eligibility) and one with `payer_type_primary = "ZZ"` (unmapped code for D-04 testing).

4. **mock_dx_parts** — list of 2 tibbles (simplified from 7). Each tibble has columns: `patid`, `encounterid`, `diagnosisid`, `dx`, `dx_type`, `dx_source`, `dx_origin`, `dx_date` (Date), `dx_poa`, `pdx`, `admit_date` (Date), `enc_type`, `providerid`, `source`. Part 1: 15 rows mapping to encounterids E001-E005 (3 dx per encounter). Part 2: 15 rows mapping to E006-E010. Include `dx_type` values "09" and "10".

5. **mock_proc_parts** — list of 2 tibbles (simplified from 4). Each tibble has columns: `patid`, `encounterid`, `proceduresid`, `px`, `px_type`, `px_source`, `px_date` (Date), `enc_type`, `providerid`. Part 1: 10 rows mapping to E001-E005. Part 2: 10 rows mapping to E006-E010.

6. **mock_provider** — tibble with 5 rows, columns: `providerid`, `provider_specialty_primary`. Use providerids matching those in encounter fixtures.

7. **mock_prov_spec** — tibble with 5 rows, columns: `provider_specialty_primary`, `cancer_provider`, `provider_classification`, `provider_specialization`. Mix of cancer_provider = 1 and 0.

All column names must be lowercase snake_case (matching post-clean_names convention from Phase 1). All character columns should be character type, all dates should be Date type. Add `library(tibble)` at top. Do NOT add `library(testthat)` — that's loaded by the test runner.
  </action>
  <verify>
    <automated>Rscript -e "source('tests/testthat/helper-fixtures.R'); stopifnot(nrow(mock_encounter1)==5, nrow(mock_encounter2)==5, length(mock_dx_parts)==2, length(mock_proc_parts)==2, nrow(mock_provider)==5, is.list(mock_sas_formats), 'p_payer' %in% names(mock_sas_formats))"</automated>
  </verify>
  <acceptance_criteria>
    - tests/testthat/helper-fixtures.R exists and is non-empty
    - File contains `mock_encounter1` with 5 rows and columns patid, encounterid, enc_type, payer_type_primary
    - File contains `mock_encounter2` with 5 rows including payer_type_primary == "4" (Dual) and payer_type_primary == "ZZ" (unmapped)
    - File contains `mock_dx_parts` as a list of length 2
    - File contains `mock_proc_parts` as a list of length 2
    - File contains `mock_provider` with 5 rows
    - File contains `mock_prov_spec` with columns cancer_provider
    - File contains `mock_sas_formats` with elements p_payer, payerr, enc_type, discharge_status, discharge_disposition
    - File contains `library(tibble)` at top
    - All column names are lowercase snake_case (no uppercase letters)
    - One encounter has NA payer_type_primary for CLN-04 testing
  </acceptance_criteria>
  <done>Helper fixtures file sources without error, creates 7 mock objects with correct dimensions and column types matching PCORnet CDM post-clean_names conventions</done>
</task>

<task type="auto">
  <name>Task 2: Create test scaffolds for cleaning and merging</name>
  <files>tests/testthat/test_02_cleaning.R, tests/testthat/test_02_merging.R</files>
  <read_first>
    - tests/testthat/helper-fixtures.R (just created in Task 1)
    - R/01_formats.R (format definitions the tests validate against)
    - .planning/phases/02-data-cleaning-merging/02-CONTEXT.md (decisions D-01 through D-08)
    - .planning/phases/02-data-cleaning-merging/02-VALIDATION.md (Wave 0 requirements)
  </read_first>
  <action>
Create two test files. Both start with:
```r
library(testthat)
library(dplyr)
source(here::here("tests", "testthat", "helper-fixtures.R"))
```

**tests/testthat/test_02_cleaning.R** — Test stubs for CLN requirements:

```r
# CLN-01: Variable names already standardized at import (verify no uppercase)
test_that("CLN-01: Column names are lowercase snake_case after import", {
  # Tests that import_sas() already handled clean_names
  expect_true(all(names(mock_encounter1) == tolower(names(mock_encounter1))))
  expect_true(all(names(mock_encounter2) == tolower(names(mock_encounter2))))
})

# CLN-02: Encounter combination (bind_rows)
test_that("CLN-02: encounter1 + encounter2 combine correctly via bind_rows", {
  combined <- bind_rows(mock_encounter1, mock_encounter2)
  expect_equal(nrow(combined), nrow(mock_encounter1) + nrow(mock_encounter2))
  expect_equal(ncol(combined), ncol(mock_encounter1))
  expect_true("patid" %in% names(combined))
  expect_true("encounterid" %in% names(combined))
})

# CLN-03: Payer type recoding using sas_formats$p_payer (per D-03)
test_that("CLN-03: Payer codes recode to grouped categories via factor()", {
  # This test will FAIL until 02_clean.R implements the recoding function
  # The function should: factor(payer_code, levels = sas_formats$p_payer$levels, labels = sas_formats$p_payer$labels)
  combined <- bind_rows(mock_encounter1, mock_encounter2)
  # Expect a function recode_payer() or inline mutate to exist
  # Test: payer code "1" -> "Medicare", "2" -> "Medicaid", "5" -> "Private"
  fmt <- mock_sas_formats$p_payer
  result <- factor(combined$payer_type_primary, levels = fmt$levels, labels = fmt$labels)
  expect_equal(as.character(result[combined$payer_type_primary == "1"]), "Medicare")
  expect_equal(as.character(result[combined$payer_type_primary == "2"]), "Medicaid")
  expect_equal(as.character(result[combined$payer_type_primary == "5"]), "Private")
  expect_equal(as.character(result[combined$payer_type_primary == "4"]), "Med_Medicaid")
})

# CLN-03 continued: Unmapped payer codes handled per D-04
test_that("CLN-03/D-04: Unmapped payer codes become NA from factor(), script warns and assigns Unknown", {
  fmt <- mock_sas_formats$p_payer
  result <- factor("ZZ", levels = fmt$levels, labels = fmt$labels)
  # factor() returns NA for unmapped codes — script must catch and assign "Unknown"
  expect_true(is.na(result))
})

# CLN-04: Missing values handled explicitly with is.na()
test_that("CLN-04: Missing payer_type_primary handled with explicit is.na()", {
  combined <- bind_rows(mock_encounter1, mock_encounter2)
  na_rows <- combined %>% filter(is.na(payer_type_primary))
  # At least one row has NA payer (from fixtures)
  expect_gt(nrow(na_rows), 0)
  # NA should NOT be silently dropped by filter operations
  expect_equal(nrow(combined), 10)
})

# CLN-05: Encounter type and discharge status recoded with factor levels
test_that("CLN-05: enc_type recodes to labeled factor via sas_formats$enc_type", {
  fmt <- mock_sas_formats$enc_type
  result <- factor(mock_encounter1$enc_type, levels = fmt$levels, labels = fmt$labels)
  expect_true(is.factor(result))
  expect_equal(as.character(result[mock_encounter1$enc_type == "AV"]), "Ambulatory Visit")
})

test_that("CLN-05: discharge_status recodes to labeled factor", {
  fmt <- mock_sas_formats$discharge_status
  result <- factor(mock_encounter1$discharge_status, levels = fmt$levels, labels = fmt$labels)
  expect_true(is.factor(result))
})

test_that("CLN-05: discharge_disposition recodes to labeled factor", {
  fmt <- mock_sas_formats$discharge_disposition
  result <- factor(mock_encounter1$discharge_disposition, levels = fmt$levels, labels = fmt$labels)
  expect_true(is.factor(result))
})

# CLN-06: Primary/secondary payer derived with correct grouping
test_that("CLN-06: Dual Medicare/Medicaid (code 4) maps to Med_Medicaid", {
  fmt <- mock_sas_formats$p_payer
  result <- factor("4", levels = fmt$levels, labels = fmt$labels)
  expect_equal(as.character(result), "Med_Medicaid")
})

test_that("CLN-06: Government payer (code 3) maps to Private per p_payer format", {
  # NOTE: In sas_formats$p_payer, code "3" maps to "Private" (study-specific decision)
  fmt <- mock_sas_formats$p_payer
  result <- factor("3", levels = fmt$levels, labels = fmt$labels)
  expect_equal(as.character(result), "Private")
})
```

**tests/testthat/test_02_merging.R** — Test stubs for MRG requirements:

```r
# MRG-01: Encounter-diagnosis join by patid + encounterid
test_that("MRG-01: Encounters join with diagnoses by patid + encounterid", {
  encounters <- bind_rows(mock_encounter1, mock_encounter2)
  dx_combined <- bind_rows(mock_dx_parts[[1]], mock_dx_parts[[2]])
  result <- left_join(encounters, dx_combined, by = c("patid", "encounterid"),
                      relationship = "one-to-many")
  # Result should have more rows than encounters (multiple dx per encounter)
  expect_gte(nrow(result), nrow(encounters))
  # All original encounter patids preserved
  expect_equal(n_distinct(result$patid), n_distinct(encounters$patid))
})

# MRG-01: Encounter-procedure join
test_that("MRG-01: Encounters join with procedures by patid + encounterid", {
  encounters <- bind_rows(mock_encounter1, mock_encounter2)
  proc_combined <- bind_rows(mock_proc_parts[[1]], mock_proc_parts[[2]])
  result <- left_join(encounters, proc_combined, by = c("patid", "encounterid"),
                      relationship = "one-to-many")
  expect_gte(nrow(result), nrow(encounters))
})

# MRG-03: Many-to-many relationship detection
test_that("MRG-03: relationship argument catches unexpected many-to-many", {
  # Create data with intentional many-to-many to verify detection
  enc_duped <- bind_rows(mock_encounter1[1, ], mock_encounter1[1, ])
  dx_duped <- bind_rows(mock_dx_parts[[1]][1:2, ], mock_dx_parts[[1]][1:2, ])
  # This should error or warn with relationship = "one-to-many"
  # because encounters table has duplicate encounterid
  expect_error(
    left_join(enc_duped, dx_duped, by = c("patid", "encounterid"),
              relationship = "one-to-many"),
    class = "dplyr_error"
  )
})

# MRG-03: Verify no Cartesian product in normal case
test_that("MRG-03: Normal join does not produce Cartesian product", {
  encounters <- bind_rows(mock_encounter1, mock_encounter2)
  dx_combined <- bind_rows(mock_dx_parts[[1]], mock_dx_parts[[2]])
  result <- left_join(encounters, dx_combined, by = c("patid", "encounterid"),
                      relationship = "one-to-many")
  # Growth should be reasonable (not >20x)
  growth_factor <- nrow(result) / nrow(encounters)
  expect_lte(growth_factor, 20)
})

# MRG-04: Post-merge assertions (assertr)
test_that("MRG-04: Post-merge data has no unexpected NA encounterids", {
  encounters <- bind_rows(mock_encounter1, mock_encounter2)
  dx_combined <- bind_rows(mock_dx_parts[[1]], mock_dx_parts[[2]])
  result <- left_join(encounters, dx_combined, by = c("patid", "encounterid"),
                      relationship = "one-to-many")
  # encounterid should never be NA after join
  expect_false(any(is.na(result$encounterid)))
})

test_that("MRG-04: Patient count preserved after merge", {
  encounters <- bind_rows(mock_encounter1, mock_encounter2)
  dx_combined <- bind_rows(mock_dx_parts[[1]], mock_dx_parts[[2]])
  result <- left_join(encounters, dx_combined, by = c("patid", "encounterid"),
                      relationship = "one-to-many")
  expect_equal(n_distinct(result$patid), n_distinct(encounters$patid))
})
```

Both test files use `library(here)` for path resolution. Both source the helper-fixtures.R file. All test names include the requirement ID prefix (CLN-01, MRG-01, etc.) for traceability.
  </action>
  <verify>
    <automated>Rscript -e "library(testthat); test_file('tests/testthat/test_02_cleaning.R'); test_file('tests/testthat/test_02_merging.R')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/testthat/test_02_cleaning.R contains `library(testthat)` and `library(dplyr)`
    - tests/testthat/test_02_cleaning.R contains `source(here::here("tests", "testthat", "helper-fixtures.R"))`
    - tests/testthat/test_02_cleaning.R contains test_that blocks for CLN-01, CLN-02, CLN-03, CLN-04, CLN-05, CLN-06
    - tests/testthat/test_02_cleaning.R contains reference to `mock_sas_formats$p_payer` per D-03
    - tests/testthat/test_02_cleaning.R contains check for payer code "4" mapping to "Med_Medicaid"
    - tests/testthat/test_02_merging.R contains `library(testthat)` and `library(dplyr)`
    - tests/testthat/test_02_merging.R contains test_that blocks for MRG-01, MRG-03, MRG-04
    - tests/testthat/test_02_merging.R contains `relationship = "one-to-many"` in join calls
    - tests/testthat/test_02_merging.R contains `expect_error` for many-to-many detection
    - Both test files execute without syntax errors (tests may fail, but no parse errors)
  </acceptance_criteria>
  <done>Both test files parse and execute without syntax errors. Tests for CLN-01 through CLN-06 and MRG-01/03/04 exist as named test_that blocks with verifiable expectations. Test runner produces test results (pass or fail) for each requirement.</done>
</task>

</tasks>

<verification>
- `Rscript -e "source('tests/testthat/helper-fixtures.R'); cat('Fixtures OK\n')"` exits 0
- `Rscript -e "library(testthat); test_file('tests/testthat/test_02_cleaning.R')"` runs without parse errors
- `Rscript -e "library(testthat); test_file('tests/testthat/test_02_merging.R')"` runs without parse errors
- All requirement IDs (CLN-01 through CLN-06, MRG-01, MRG-03, MRG-04) appear in test names
</verification>

<success_criteria>
Three test infrastructure files exist in tests/testthat/. Fixtures provide realistic mock PCORnet CDM data. Test scaffolds cover all 10 Phase 2 requirements with named test_that blocks. Test runner executes without syntax errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-cleaning-merging/02-01-SUMMARY.md`
</output>
